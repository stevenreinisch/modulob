/*
 * generated by Xtext
 */
package de.dubmas.modulob.scoping;

import java.util.LinkedList;
import java.util.List;

import org.eclipse.emf.ecore.EReference;
import org.eclipse.xtext.naming.IQualifiedNameProvider;
import org.eclipse.xtext.naming.QualifiedName;
import org.eclipse.xtext.resource.EObjectDescription;
import org.eclipse.xtext.resource.IEObjectDescription;
import org.eclipse.xtext.scoping.IScope;
import org.eclipse.xtext.scoping.impl.SimpleScope;

import de.dubmas.modulob.Method;
import de.dubmas.modulob.InterfaceDslNameProvider;
import de.dubmas.modulob.behaviour.Reaction;
import de.dubmas.modulob.common.scoping.DslScopeProvider;

/**
 * This class contains custom scoping description.
 * 
 * see : http://www.eclipse.org/Xtext/documentation/latest/xtext.html#scoping
 * on how and when to use it 
 *
 */
public class BehaviourDslScopeProvider extends DslScopeProvider {
	
	private IQualifiedNameProvider nameProvider = new InterfaceDslNameProvider();
	
	public IScope scope_Reaction_method(Reaction r, EReference ref){	
		
		List<IEObjectDescription> scopedElements = new LinkedList<IEObjectDescription>();
		for(Method m: r.getInterface().getMethods()){
			QualifiedName qn = nameProvider.getFullyQualifiedName(m);
			scopedElements.add(EObjectDescription.create(qn, m));
		}
	
		return new SimpleScope(scopedElements);
	}
}
